-----

# Linux Native Input Support / Linuxでのネイティブ入力対応

This application uses **Zenity** to ensure stable IME (Input Method) support on Linux.
If the text input dialog does not appear, please install `zenity`.

本アプリはLinux環境でのIME（日本語入力など）を安定させるため、**Zenity** を使用しています。
もし文字入力ダイアログが表示されない場合は、以下の手順に従って `zenity` をインストールしてください。

-----

## 🇯🇵 日本語 (Japanese)

**日本語入力ダイアログが表示されない場合:**
本アプリはIME入力を確実に行うため、OS標準の `zenity` を使用します。Ubuntu等の多くの環境では標準搭載されていますが、もし入力画面が出ない場合は以下のコマンドでインストールしてください。

  * **Ubuntu / Debian / Linux Mint:**
    ```bash
    sudo apt install zenity
    ```
  * **Arch Linux / Manjaro:**
    ```bash
    sudo pacman -S zenity
    ```
  * **Fedora:**
    ```bash
    sudo dnf install zenity
    ```

-----

## 🇨🇳 简体中文 (Simplified Chinese)

**如果未显示输入对话框:**
本程序使用 `zenity` 来确保输入法 (IME) 的稳定工作。大多数 Linux 发行版已默认安装。如果无法输入文字，请执行以下命令安装 `zenity`。

  * **Ubuntu / Debian / Deepin:**
    ```bash
    sudo apt install zenity
    ```
  * **Arch Linux / Manjaro:**
    ```bash
    sudo pacman -S zenity
    ```
  * **Fedora:**
    ```bash
    sudo dnf install zenity
    ```

-----

## 🇰🇷 한국어 (Korean)

**입력 대화상자가 나타나지 않는 경우:**
이 애플리케이션은 안정적인 IME(한글 입력) 지원을 위해 `zenity`를 사용합니다. 입력 창이 뜨지 않는다면, 다음 명령어를 사용하여 `zenity`를 설치해 주세요.

  * **Ubuntu / Debian:**
    ```bash
    sudo apt install zenity
    ```
  * **Arch Linux:**
    ```bash
    sudo pacman -S zenity
    ```
  * **Fedora:**
    ```bash
    sudo dnf install zenity
    ```

-----

## 🇮🇳 हिन्दी (Hindi)

**यदि इनपुट डायलॉग दिखाई नहीं दे रहा है:**
यह एप्लिकेशन IME इनपुट को सुनिश्चित करने के लिए `zenity` का उपयोग करता है। यदि आप टेक्स्ट इनपुट नहीं कर पा रहे हैं, तो कृपया निम्नलिखित कमांड का उपयोग करके `zenity` स्थापित करें।

  * **Ubuntu / Debian:**
    ```bash
    sudo apt install zenity
    ```
  * **Arch Linux:**
    ```bash
    sudo pacman -S zenity
    ```

-----

## 🇸🇦 العربية (Arabic)

**في حال عدم ظهور مربع الحوار للإدخال:**
يستخدم هذا التطبيق `zenity` لضمان استقرار الكتابة باستخدام نظام الإدخال (IME). إذا لم تظهر نافذة الإدخال، يرجى تثبيت `zenity` باستخدام الأمر التالي.

  * **Ubuntu / Debian:**
    ```bash
    sudo apt install zenity
    ```
  * **Arch Linux:**
    ```bash
    sudo pacman -S zenity
    ```

-----

## 🇷🇺 Русский (Russian)

**Если диалоговое окно ввода не появляется:**
Приложение использует `zenity` для стабильной работы ввода текста. Если окно ввода не открывается, установите `zenity` следующей командой.

  * **Ubuntu / Debian:** `sudo apt install zenity`
  * **Arch Linux:** `sudo pacman -S zenity`
  * **Fedora:** `sudo dnf install zenity`
